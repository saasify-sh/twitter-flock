'use strict'

module.exports = async function findEmailForTwitterUser() {
  // const { name, screen_name: screenName, description: desc, entities } = user
  // const relatedUrls = []
  // for (const entity of Object.keys(entities)) {
  //   const { urls = [] } = entities[entity]
  //   for (const urlInfo of urls) {
  //     relatedUrls.push(new URL(urlInfo.expanded_url))
  //   }
  // }
  // TODO
}
